{
  "enabled": true,
  "blocked": [],
  "_comment": "KILL SWITCH. enabled=false desliga o botao em TODAS as sub-accounts em ate 15min (ou no proximo reload da aba). blocked = lista de location_ids a desligar individualmente. Se este arquivo ficar fora do ar, o script assume DESLIGADO (fail-closed)."
}
